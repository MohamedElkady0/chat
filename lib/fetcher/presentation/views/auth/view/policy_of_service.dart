import 'package:flutter/material.dart';

class PolicyOfService extends StatelessWidget {
  const PolicyOfService({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
